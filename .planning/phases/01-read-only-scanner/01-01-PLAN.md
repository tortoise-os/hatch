---
phase: 01-read-only-scanner
type: execute
domain: sui-defi
---

<objective>
Build a one-shot Rust scanner that compares live Cetus and Bluefin/7K quote surfaces and ranks SUI-to-USDC-to-SUI round trips.

Purpose: Replace Hatch's synthetic monitor with auditable read-only market evidence before any executor work.
Output: `apps/scanner` Rust binary, unit tests, usage documentation, and phase summary.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within scanner and documentation scope, run every verification command, and never add signing or transaction-submission behavior. After tasks pass, write SUMMARY.md with Outcomes, Files changed, Verification, and Follow-ups sections.
</execution_context>

<context>
Read these project files before editing:
- .planning/BRIEF.md
- .planning/ROADMAP.md
- docs/plans/2026-09-11-sui-read-only-scanner-design.md
- apps/bot/src/monitor/dex-monitor.ts
- apps/bot/config/bot.config.json
- packages/strategy-sdk/src/arbitrage/arbitrage-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create normalized scanner core</name>
  <files>apps/scanner/Cargo.toml, apps/scanner/src/lib.rs, apps/scanner/src/model.rs, apps/scanner/src/provider.rs, apps/scanner/src/scanner.rs</files>
  <action>Create exact-in quote model, route metadata, provider failure records, round-trip candidates, and scan report. Define object-safe async quote provider. Calculate signed gross/net profit and basis points with checked integer arithmetic. Fan out forward and reverse requests with bounded concurrency. Keep candidates from partial provider success. Avoid floating point and execution types because outputs must remain auditable signals.</action>
  <verify>cargo test --manifest-path apps/scanner/Cargo.toml scanner</verify>
  <done>Tests prove profitable, losing, threshold, deterministic ranking, and partial-provider scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Add current Cetus and Bluefin/7K adapters</name>
  <files>apps/scanner/src/providers/mod.rs, apps/scanner/src/providers/cetus.rs, apps/scanner/src/providers/seven_k.rs, apps/scanner/src/http.rs, apps/scanner/tests/fixtures/cetus-quote.json, apps/scanner/tests/fixtures/seven-k-quote.json</files>
  <action>Implement published read-only quote requests: Cetus `router_v3/find_routes` and Bluefin/7K `v3/quote`. Normalize atomic amounts, quote IDs, venue names, and pool IDs. Add configurable endpoints/source filters, request timeout, bounded retry for timeout/429/5xx, and immediate failure for malformed/permanent responses. Use fixture responses derived from current published response contracts; never import transaction-building SDKs.</action>
  <verify>cargo test --manifest-path apps/scanner/Cargo.toml providers</verify>
  <done>Both fixtures parse into equivalent normalized quotes, errors identify provider and failure class, and no signing dependency exists.</done>
</task>

<task type="auto">
  <name>Task 3: Ship CLI and verify live read-only scan</name>
  <files>apps/scanner/src/main.rs, apps/scanner/README.md, .planning/phases/01-read-only-scanner/01-01-SUMMARY.md</files>
  <action>Add one-shot CLI with configurable coin types, amount, gas reserve, threshold, timeouts, retries, endpoints, and source filters. Default to native SUI and native USDC. Emit one compact JSON object per scan. Return non-zero only when no complete round trip exists or configuration is invalid. Document signal limitations and exact commands. Run format, tests, clippy, and live smoke request.</action>
  <verify>cargo fmt --manifest-path apps/scanner/Cargo.toml --check && cargo test --manifest-path apps/scanner/Cargo.toml && cargo clippy --manifest-path apps/scanner/Cargo.toml --all-targets -- -D warnings && cargo run --manifest-path apps/scanner/Cargo.toml -- --amount 1000000000 --timeout-ms 5000 --retries 1</verify>
  <done>Command emits valid single-line JSON without wallet credentials; output contains candidates or precise provider errors and route freshness metadata.</done>
</task>

</tasks>

<verification>
- [ ] `cargo fmt --manifest-path apps/scanner/Cargo.toml --check`
- [ ] `cargo test --manifest-path apps/scanner/Cargo.toml`
- [ ] `cargo clippy --manifest-path apps/scanner/Cargo.toml --all-targets -- -D warnings`
- [ ] Live mainnet smoke scan emits parseable one-line JSON.
- [ ] Scanner manifest and CLI contain no signer, keypair, wallet, or transaction-execution dependency.
</verification>

<success_criteria>
- Both current quote services produce normalized route evidence.
- All profit calculations use atomic integer units.
- Partial upstream failure remains visible and does not discard other complete routes.
- Scanner remains structurally unable to submit transactions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-only-scanner/01-01-SUMMARY.md` with Accomplishments, Files Created/Modified, Decisions Made, Issues Encountered, Verification, and Next Step.
</output>
