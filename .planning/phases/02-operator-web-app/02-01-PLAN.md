---
phase: 02-operator-web-app
plan: 01
type: execute
domain: rust-http-api
---

<objective>
Expose scanner through bounded read-only HTTP API.

Purpose: Let browser trigger scans and inspect health/history without weakening scanner safety boundary.
Output: Axum service mode, API state and handlers, handler tests.
</objective>

<execution_context>
Execute tasks in order. Read listed context files first. Keep changes within task scope, run each verification command, and write summary after completion.
</execution_context>

<context>
- docs/plans/2026-09-11-hatch-operator-web-app-design.md
- apps/scanner/src/main.rs
- apps/scanner/src/scanner.rs
- apps/scanner/src/model.rs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract reusable scanner construction and API models</name>
  <files>apps/scanner/src/app.rs, apps/scanner/src/lib.rs, apps/scanner/src/main.rs, apps/scanner/Cargo.toml</files>
  <action>Add shared settings and provider construction, server CLI mode, Axum dependencies, input validation, scan lock, and capped in-memory report history. Keep decimal atomic values and avoid transaction-capable dependencies.</action>
  <verify>cargo test --manifest-path apps/scanner/Cargo.toml</verify>
  <done>CLI and service use same scanner core; only one scan can run; history caps at 100.</done>
</task>
<task type="auto">
  <name>Task 2: Add API handlers and contract tests</name>
  <files>apps/scanner/src/api.rs, apps/scanner/src/lib.rs</files>
  <action>Implement health, config, create scan, latest, and history endpoints with typed errors and permissive localhost CORS. Add router tests for validation, empty latest, health, and history behavior.</action>
  <verify>cargo test --manifest-path apps/scanner/Cargo.toml api</verify>
  <done>API contract matches approved design and contains no execution route.</done>
</task>
</tasks>

<verification>
- cargo fmt --manifest-path apps/scanner/Cargo.toml --check
- cargo test --manifest-path apps/scanner/Cargo.toml
- cargo clippy --manifest-path apps/scanner/Cargo.toml --all-targets -- -D warnings
</verification>

<success_criteria>Rust API runs locally, returns typed JSON, preserves partial failures, and stays read-only.</success_criteria>
<output>Create 02-01-SUMMARY.md with outcomes, files, verification, and follow-ups.</output>
