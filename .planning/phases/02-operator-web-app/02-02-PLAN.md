---
phase: 02-operator-web-app
plan: 02
type: execute
domain: frontend-design
---

<objective>
Build TortoiseOS operator frontend.

Purpose: Turn scanner JSON into fast overview and evidence-rich scanner workflow.
Output: Next.js app at apps/web with overview, scanner, theme, API client, responsive components.
</objective>

<execution_context>Read context, implement tasks in order, verify each, and write summary.</execution_context>

<context>
- .impeccable.md
- docs/plans/2026-09-11-hatch-operator-web-app-design.md
- apps/scanner/src/model.rs
- .planning/phases/02-operator-web-app/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create application shell and data layer</name>
  <files>apps/web/package.json, apps/web/src/app/layout.tsx, apps/web/src/app/globals.css, apps/web/src/lib/api.ts, apps/web/src/components/app-shell.tsx</files>
  <action>Create Next.js app, TortoiseOS token system, system-aware persisted theme toggle, typed API client using bigint-safe decimal strings, sticky header, health and READ ONLY state. Avoid wallet and transaction dependencies.</action>
  <verify>bun run --cwd apps/web typecheck</verify>
  <done>Shell loads in both themes and typed API boundary matches Rust models.</done>
</task>
<task type="auto">
  <name>Task 2: Build overview and scanner workflows</name>
  <files>apps/web/src/app/page.tsx, apps/web/src/app/scanner/page.tsx, apps/web/src/components/dashboard.tsx</files>
  <action>Build overview metrics/history and scanner control-plus-results workspace with candidates, routes, failures, history, responsive card/table behavior, loading/error/empty states, accessible tabs and disclosures.</action>
  <verify>bun run --cwd apps/web build</verify>
  <done>Both routes build, trigger scans, preserve prior results during refresh, and show evidence without execution controls.</done>
</task>
</tasks>

<verification>
- bun run --cwd apps/web typecheck
- bun run --cwd apps/web build
- repository search finds no wallet/sign/execute dependency in apps/web
</verification>

<success_criteria>Operator can inspect health, run scan, review candidates/routes/failures/history, and toggle accessible themes.</success_criteria>
<output>Create 02-02-SUMMARY.md with outcomes, files, verification, and follow-ups.</output>
